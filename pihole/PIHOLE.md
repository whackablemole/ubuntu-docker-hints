# Pihole Setup on Ubuntu

## Troubleshooting

If you get an error when creating the Macvlan network that says "operation not supported", then run the following command

```
sudo apt install linux-modules-extra-raspi 
```